#include<stdio.h> // first number is 10 and second number is 15 now add two numbers
int main() //main function
{
    int num1=10,num2=15, add; // num1 and num2 are variables and int is datatype
    add=num1+num2;
    printf("Addition of the two number is %d",add); // %d is formate specifier
}
